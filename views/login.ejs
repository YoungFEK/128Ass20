<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Login Page</title>
    <style>
        /* Coffee Themed Login Page - Matching the Todo List Style */

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    display: flex;
    height: 100vh;
    justify-content: center;
    align-items: center;
    background: linear-gradient(to bottom right, #f5f1e8, #e8e0d1) !important;
    background-size: cover;
    padding: 20px;
}

.container {
    display: flex;
    width: 850px;
    height: 500px;
    background: #fffaf0;
    border-radius: 15px;
    box-shadow: 0 8px 25px rgba(139, 107, 77, 0.2);
    overflow: hidden;
    border: 2px solid #d4c4a8;
}

/* LEFT SIDE - Coffee Illustration */
.illustration {
    flex: 1;
    background: linear-gradient(135deg, #6f4e37, #8b6b4d) !important;
    background-size: cover;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 2rem;
    color: #fff;
    position: relative;
    overflow: hidden;
}

.illustration::before {
    content: "☕";
    position: absolute;
    font-size: 8rem;
    opacity: 0.15;
    transform: rotate(-15deg);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.illustration h2 {
    font-size: 1.8rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
    color: #f9f5eb;
    position: relative;
    z-index: 1;
}

.illustration img {
    width: 70%;
    max-width: 250px;
    height: auto;
    margin: 0.5rem 0;
    object-fit: contain;
    filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.2));
    position: relative;
    z-index: 1;
}

.illustration p {
    font-size: 1rem;
    color: #f2ecdd;
    margin-top: 1.5rem;
    width: 80%;
    line-height: 1.5;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
    font-style: italic;
    position: relative;
    z-index: 1;
}

/* RIGHT SIDE - Login Box */
.login-box {
    flex: 1;
    padding: 3rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    background: #fffefc;
    color: #3d2c1f;
}

.login-box h1 {
    font-size: 2.5rem;
    font-weight: 700;
    color: #6f4e37;
    margin-bottom: 0.5rem;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
}

.login-box h3 {
    color: #8d7b6d;
    font-size: 1rem;
    margin-bottom: 2rem;
    font-weight: 500;
}

label {
    font-size: 0.95rem;
    color: #5a3f2b;
    margin-bottom: 0.5rem;
    display: block;
    font-weight: 600;
}

input[type="text"],
input[type="password"] {
    width: 100%;
    padding: 12px 18px;
    margin-bottom: 1.5rem;
    border: 2px solid #d4c4a8;
    border-radius: 10px;
    font-size: 1rem;
    background: #f9f5eb;
    color: #3d2c1f;
    transition: all 0.3s ease;
}

input[type="text"]:focus,
input[type="password"]:focus {
    outline: none;
    border-color: #8b6b4d;
    box-shadow: 0 0 0 3px rgba(139, 107, 77, 0.2);
    background: #fffefc;
}

.forgot {
    text-align: right;
    margin-top: -0.8rem;
    margin-bottom: 1.5rem;
}

.forgot a {
    color: #8b6b4d;
    font-size: 0.9rem;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.3s;
}

.forgot a:hover {
    color: #6f4e37;
    text-decoration: underline;
}

/* Buttons */
button {
    padding: 14px;
    border: none;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 1rem;
    margin-top: 0.5rem;
}

button[type="submit"] {
    background: linear-gradient(135deg, #8b6b4d, #6f4e37);
    color: white;
    box-shadow: 0 4px 12px rgba(139, 107, 77, 0.3);
}

button[type="submit"]:hover {
    background: linear-gradient(135deg, #6f4e37, #5a3f2b);
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(107, 77, 54, 0.4);
}

#signupButton {
    background: #f2ecdd;
    color: #6f4e37;
    border: 2px solid #8b6b4d !important;
    box-shadow: 0 4px 8px rgba(139, 107, 77, 0.15);
}

#signupButton:hover {
    background: #8b6b4d;
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(139, 107, 77, 0.3);
}

/* Divider */
.or {
    text-align: center;
    margin: 1.5rem 0;
    color: #8d7b6d;
    position: relative;
    font-weight: 500;
}

.or::before,
.or::after {
    content: "";
    position: absolute;
    height: 1px;
    width: 35%;
    background: linear-gradient(to right, transparent, #d4c4a8, transparent);
    top: 50%;
}

.or::before {
    left: 0;
}

.or::after {
    right: 0;
}

/* Alerts */
.alert {
    padding: 12px;
    border-radius: 8px;
    margin-bottom: 1.5rem;
    text-align: center;
    font-weight: 500;
}

.alert-success {
    background: #e8f5e9;
    color: #2e7d32;
    border: 2px solid #c8e6c9;
}

.alert-error {
    background: #ffebee;
    color: #c62828;
    border: 2px solid #ffcdd2;
}

/* Responsive Design */
@media (max-width: 900px) {
    body {
        padding: 15px;
        height: auto;
        min-height: 100vh;
    }
    
    .container {
        flex-direction: column;
        width: 100%;
        max-width: 500px;
        height: auto;
        margin: 20px 0;
    }
    
    .illustration {
        padding: 2rem 1rem;
        min-height: 200px;
    }
    
    .illustration h2 {
        font-size: 1.5rem;
    }
    
    .illustration img {
        width: 50%;
        max-width: 200px;
    }
    
    .illustration p {
        width: 90%;
        font-size: 0.9rem;
    }
    
    .login-box {
        padding: 2rem 1.5rem;
    }
    
    .login-box h1 {
        font-size: 2rem;
    }
}

@media (max-width: 480px) {
    .container {
        border-radius: 12px;
    }
    
    .login-box {
        padding: 1.5rem;
    }
    
    button {
        padding: 12px;
    }
    
    input[type="text"],
    input[type="password"] {
        padding: 10px 15px;
    }
}

/* Optional: Add some coffee-themed decorative elements */
.login-box::after {
    content: "✧";
    position: absolute;
    bottom: 20px;
    right: 20px;
    color: #d4c4a8;
    font-size: 1.5rem;
    opacity: 0.5;
}
    </style>
</head>
<body>
<div class="container">
    <div class="illustration">
        <h2>Welcome to</h2>
        <img src="/source/CoffeeText.png" alt="Coffee Text" />
        <img src="/source/TimeText.png" alt="Time Text" />
        <p>Relax, sip, and enjoy your favorite brew while logging in.</p>
    </div>

    <div class="login-box">
        <h1>Log In</h1>
        <h3>Welcome Back!</h3>
        <form action="/loginPage" method="POST">
            <!-- <form action="/login" method="POST"></form> -->
            <label for="username">Username</label>
            <input type="text" name="username" id="username" required />

            <label for="password">Password</label>
            <input type="password" name="password" id="password" required />

            <div class="forgot">
                <a href="/forgot-Password">Forgot password?</a>
            </div>

            <button type="submit">Sign in</button>

            <div class="or">or</div>

            <button type="button" onclick="window.location='/register'" id="signupButton">Sign Up</button>
        </form>
    </div>
</div>
<script>
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.get('registered') === 'success') {
        alert('Registered successfully!');
    }
    if (urlParams.get('message') === 'resetSuccess') {
        alert('Reset  successfully!');
    }
     if (urlParams.get('error') === 'invalid') {
        alert('Incorrect Username or Password');
    }
</script>
</body>
</html>

